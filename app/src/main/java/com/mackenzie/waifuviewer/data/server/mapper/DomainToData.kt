package com.mackenzie.waifuviewer.data.server.mapper

import com.mackenzie.waifuviewer.data.db.WaifuImDbItem
import com.mackenzie.waifuviewer.data.server.models.ArtistImResult
import com.mackenzie.waifuviewer.data.server.models.Tag
import com.mackenzie.waifuviewer.domain.im.ArtistIm
import com.mackenzie.waifuviewer.domain.im.TagItem
import com.mackenzie.waifuviewer.domain.im.WaifuImItem

/*fun List<WaifuImItem>.fromDomainModel(): List<WaifuImDbItem> = map { it.fromDomainModel() }

fun WaifuImItem.fromDomainModel(): WaifuImDbItem = WaifuImDbItem(
    id,
    artist.fromDomainModel(),
    byteSize,
    signature,
    extension,
    dominantColor,
    source,
    uploadedAt,
    isNsfw,
    width,
    height,
    imageId,
    url,
    previewUrl,
    tags.fromDomainModel(),
    isFavorite
)*/

/*
fun ArtistIm.fromDomainModel(): ArtistImResult = ArtistImResult(
    artistId,
    deviantArt,
    name,
    patreon,
    pixiv,
    twitter
)


fun List<TagItem>.fromDomainModel() : List<Tag> = map { it.fromDomainModel()}

fun TagItem.fromDomainModel(): Tag = Tag(
    description,
    isNsfw,
    name,
    tagId
)*/
